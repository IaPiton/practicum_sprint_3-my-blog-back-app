version: '3.8'

services:
  db-blog:
    container_name: db-blog
    image: postgres:17.5
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - ./migration/:/docker-entrypoint-initdb.d/